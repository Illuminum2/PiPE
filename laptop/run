# Fix bug with wrong directory
cd "$(dirname "$0")"

source .venv/bin/activate
uvicorn position_api:app --host 0.0.0.0 --port 8000 &
api_pid=$!

trap "kill $api_pid" EXIT

nginx -p "$PWD" -c nginx.conf -g "daemon off;"